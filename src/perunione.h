#ifndef PERUNIONE_H
#define PERUNIONE_H

/*
 * perunione — single header for library consumers.
 *
 * Usage:
 *   #include "perunione.h"
 *   // perunione_context, Packet, all proto_*() functions available
 */

#include "proto_logic.h"

#endif
